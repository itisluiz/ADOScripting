import { DevOps } from "../src/interfaces/devops";
import { Logger } from "../src/interfaces/logger";

export async function script(_payload: any, _devops: DevOps, _logger: Logger) {
	// Scripts will be interpreted as bodies for this function
}
